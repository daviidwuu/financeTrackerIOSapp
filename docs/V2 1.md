
v2.1 Social & Group Finance - Task Breakdown
Timeline: 3-4 weeks (21 implementation days + 5 testing days)
Strategy: Fresh Start (Abandon legacy private groups/requests)

📋 Phase 1: Foundation - Data Models & Rules
Duration: 3 days
Risk: Medium (Breaking changes to core models)

Data Models (FirestoreModels.swift)
 Add isGuest, guestId, isAccepted fields to 
Split
 model
 Create new Guest model with avatarColor, totalOwed, userId
 Create new FriendRequest model with fromUid, toUid, status
 Create new GroupInvitation model with groupId, groupName, dependencyId
 Update Group model: rename memberIds → members, add normalizedName, createdBy, updatedAt
 Update 
SplitRequest
 model: add groupId, rename requesterId → fromUid, add toUid, dependencyId, lastNudgedAt, change status to enum
Firestore Security Rules (firestore.rules)
 Add root collection rules for groups (wiki-style with member access)
 Add root collection rules for split_requests (fromUid/toUid access only)
 Add root collection rules for friend_requests (sender/receiver access)
 Add root collection rules for group_invitations (sender/receiver access)
 Add private subcollection rules for users/{uid}/guests
 Set legacy users/{uid}/groups and users/{uid}/requests to read-only
 Deploy updated rules to Firebase Console
📋 Phase 2: Friend Request Flow
Duration: 3 days
Risk: Medium (Changes user-facing friend-add behavior)

New Repository (FriendRequestRepository.swift)
 Create FriendRequestRepository.swift with incomingRequests and outgoingRequests publishers
 Implement startListening() with dual queries (toUid and fromUid)
 Implement sendFriendRequest(fromUid:toUid:) method
 Implement acceptRequest() and declineRequest() methods
 Implement stopListening() cleanup
Update FriendRepository.swift
 Deprecate addFriend() method, redirect to sendFriendRequest()
 Add isFriend(uid:) -> Bool method for cache lookup
Update AppState.swift
 Add @Published var friendRepo = FriendRepository() (global cache)
 Add @Published var friendRequestRepo = FriendRequestRepository()
 Update init() to start listening to friendRepo on login
 Update init() to start listening to friendRequestRepo on login
 Add stopListening() calls for both repos on logout
📋 Phase 3: Group Repository Migration
Duration: 2 days
Risk: High (Existing groups become invisible)

Update GroupRepository.swift
 Change startListening() from users/{uid}/groups → groups root with .whereField("members", arrayContains: uid)
 Update addGroup() to write to root collection
 Add normalizedName generation in addGroup()
 Add createdBy and updatedAt fields in addGroup()
 Ensure current user is in members array before saving
 Add isMember(groupId:uid:) -> Bool method for cache lookup
 Update updateGroup() to use root collection path
 Update deleteGroup() to use root collection path
📋 Phase 4: Guest System
Duration: 4 days
Risk: Low (New feature, no breaking changes)

New Repository (GuestRepository.swift)
 Create GuestRepository.swift with guests publisher
 Implement startListening() for users/{uid}/guests subcollection
 Implement createGuest(name:) with random avatarColor generation
 Implement findMatchingGuests(friendName:) with substring matching (Decision #3)
 Implement stopListening() cleanup
Update SplitConfigurationView (TransactionDetailView.swift)
 Add @StateObject var guestRepo = GuestRepository()
 Add computed matchingFriends property for filtered results
 Add computed matchingGuests property for filtered results
 Update "Add People" section to show 3 sections: Friends, Guests, "+ Create Guest"
 Implement createAndAddGuest(name:) method for inline creation
 Implement addFriendToSplit() and addGuestToSplit() helper methods
 Start listening to guestRepo in .onAppear
Smart Intercept Modal (FriendsListView.swift)
 Change "Add" button text to "Send Request"
 Implement checkForGuestDuplicates(targetUser:) method
 Implement showSmartInterceptAlert(guest:targetUser:) with UIAlertController
 Add "Yes, Merge & Add" and "No, Just Add" actions
 Call checkForGuestDuplicates() before sending friend request
📋 Phase 5: Client-Side Batch Logic
Duration: 3 days
Risk: Medium (Complex batch write logic)

New Manager (SocialTransactionManager.swift)
 Create SocialTransactionManager.swift class
 Implement createSocialTransaction() with 6 parameters (payerUid, splits, transactionId, groupId, friendCache, groupCache)
 Add STEP A: Friend request creation logic (check cache, create if not friend)
 Add STEP B: Group invitation logic (check membership, create with dependency if needed)
 Add STEP C: Split request creation logic (determine status based on blocking)
 Add atomic batch.commit() with error propagation (Decision #6)
Update TransactionDetailView.swift
 Update saveSplits() to call SocialTransactionManager.createSocialTransaction()
 Pass appState.friendRepo.friends as friendCache (Decision #4)
 Pass appState.groupRepo.groups as groupCache
 Add error handling with showErrorAlert() (Decision #6)
 Add @State var showErrorAlert and @State var errorMessage
 Add .alert("Error", isPresented: $showErrorAlert) to view body
 Ensure transaction is NOT saved if batch fails
📋 Phase 6: Cloud Functions
Duration: 4 days
Risk: High (Critical cascade logic)

Cascade Triggers (functions/index.js)
 Add onFriendRequestUpdated trigger: cascade to group invites when accepted
 Add bidirectional friend add to users/{uid}/friends subcollection in cascade
 Add onGroupInvitationUpdated trigger: cascade to split requests when accepted
 Add member to groups/{groupId}.members array in cascade
 Add onSplitRequestAccepted trigger: sync isAccepted back to Transaction.splits
Dual Notification System (Decision #1)
 Create sendNotification() helper function with fcmToken lookup
 Create getUserName() helper function
 Add notifyFriendRequest onCreate trigger for immediate pending requests
 Add notifyGroupInvite onUpdate trigger for status change (blocked → pending)
 Add notifySplitRequest onUpdate trigger for status change (blocked → pending)
 Keep legacy onSplitRequestCreatedLegacy for backward compatibility
 Deploy all Cloud Functions to Firebase
Testing Cloud Functions Locally
 Test friend request cascade with Firebase Emulator Suite
 Test group invitation cascade with emulator
 Test split request acceptance sync with emulator
 Test dual notification triggers with emulator
📋 Phase 7: UI Updates
Duration: 2 days
Risk: Low (Cosmetic changes)

FriendsListView.swift
 Add "Incoming Requests" section header
 Display appState.friendRequestRepo.incomingRequests in list
 Add "Accept" button calling friendRequestRepo.acceptRequest()
 Add "Decline" button calling friendRequestRepo.declineRequest()
 Show "No pending requests" placeholder when empty
 Add "Outgoing Requests" section (optional) for sent requests
TransactionDetailView.swift - Split Display
 Add isAccepted badge (green checkmark seal) next to split name
 Conditionally show badge only when split.isAccepted == true
 Update split row layout to accommodate badge
 Add tooltip or label explaining "Request Accepted" status
📋 Testing & Verification
Duration: 5 days
Risk: High (User-facing validation)

Manual Test Scenarios
 Test 1: Friend Request Flow (send, receive notification, accept, verify cascade)
 Test 2: Group Creation (wiki-style, collaborative editing)
 Test 3: Split with Dependency Chain (blocked → pending cascade, delayed notifications)
 Test 4: Guest Creation (inline, no split request sent)
 Test 5: Smart Intercept (duplicate detection, merge decision)
 Test 6: Dual Notifications (onCreate vs onUpdate triggers)
 Test 7: Error Handling (offline, batch failure, retry)
Edge Cases
 Test with user who has 0 friends (cold start)
 Test with user who has 50+ friends (cache performance)
 Test with user who creates 10 split requests at once (batch size)
 Test with unstable internet connection (partial failures)
 Test with legacy data (read-only access to old groups/requests)
🚨 Deployment Checklist
Pre-Deployment
 Review all Firestore rule changes in Firebase Console
 Backup production Firestore data (export to GCS)
 Create rollback plan document
 Test all Cloud Functions in emulator
 Verify FCM token infrastructure is working
Deployment Sequence
 Deploy Firestore rules (includes 30-day read-only for legacy)
 Deploy Cloud Functions (includes dual triggers)
 Deploy iOS app update to TestFlight (beta group)
 Monitor error logs for 48 hours
 Deploy iOS app to App Store (gradual rollout)
Post-Deployment Monitoring
 Monitor Cloud Function execution logs for errors
 Monitor Firestore read/write quotas (expect increase due to root collections)
 Monitor push notification delivery rates
 Collect user feedback on friend request flow
 Track legacy data access (should decrease over 30 days)
📊 Progress Summary
Total Tasks: 108 items
Completed: 0
In Progress: 0
Blocked: 0

Phase Status:

Phase 1 (Foundation): ⬜️ Not Started
Phase 2 (Friend Requests): ⬜️ Not Started
Phase 3 (Group Migration): ⬜️ Not Started
Phase 4 (Guest System): ⬜️ Not Started
Phase 5 (Client Batch): ⬜️ Not Started
Phase 6 (Cloud Functions): ⬜️ Not Started
Phase 7 (UI Updates): ⬜️ Not Started
Testing & Verification: ⬜️ Not Started

does this conflict this?